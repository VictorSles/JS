// Script basico usando variaveis compostas para criar dados sequenciais

var valores = [1,2,3,4,5,6,7,8]
posicao = 0
for (posicao; posicao < valores.length ; posicao++){
    console.log(`A posicao ${posicao} tem o valor: ${valores[posicao]}`)
}